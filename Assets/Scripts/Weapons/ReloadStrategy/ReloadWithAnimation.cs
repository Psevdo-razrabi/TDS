using Game.Player.Weapons.InterfaseWeapon;
using UI.Storage;

namespace Game.Player.Weapons.ReloadStrategy
{
    public class ReloadWithAnimation : IReloadStrategy
    {
        public void Reload(ReloadComponent reloadComponent, BoolStorage boolStorage)
        {
            throw new System.NotImplementedException();
        }
    }
}